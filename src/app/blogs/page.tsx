import Blogs_Headers from '@/components/CSR/Navbar/Blogs_Headers';
import React from 'react';

export default function Blog_Page() {

	return (
		<section
			className={ `flex-full-center h-full w-3/4 mx-auto` }
		>
			<Blogs_Headers/>
		</section>
	);
}